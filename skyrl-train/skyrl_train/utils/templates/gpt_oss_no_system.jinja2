{# Minimal GPT-OSS-style chat template with NO injected system prompt.
   This overrides the default template which always renders a system message.

   Notes:
   - This uses the GPT-OSS special tokens: <|start|>role<|message|> ... <|end|>
   - When add_generation_prompt=True, we append an assistant header and leave it open-ended.
 #}

{%- for message in messages -%}
{{- "<|start|>" + message["role"] + "<|message|>" + message["content"] + "<|end|>" -}}
{%- endfor -%}

{%- if add_generation_prompt -%}
{{- "<|start|>assistant<|message|>" -}}
{%- endif -%}

